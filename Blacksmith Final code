-- The Final-code-- The Final-code
-- Table of contents:
-- All diamond items = 50$ each
-- All Gold items = 35$ each
-- All iron items = 20% each
-- All cobblestone items = 15$ each
-- All wooden items- = 5$ each
-- Welcome, the following will be my submission for term 3, and I veru much ope that it works...
B = "minecraft:diamond"
C = "minecraft:gold_ingot"
D = "minecraft:iron_ingot"
-- The above shall be the only legitamate means to purchase the items of your chosing and below, I will showhow much each one is worth.
b = 50
c = 35
d = 15
-- If you'd like to purcahse diamond items, please pay with slot 5, gold for slot 6 and to pay for iron use slot 7
-- Now, i will begin with the code which will permit the user to give an currency to the turtle and receive an item back.
function Diamond_process()
-- in this function, you will be permitted to purchase any of the diamond items, for they are all worth the same...
turtle.select(5)
-- The above will select the slot in which people will be inserting their currency.
turtle.getSelectedSlot(5)
    if turtle.getItemSpace(5) = b then
    -- The above two will check to make sure that the slot is good and will count the amount of itmes in the selected slot. 
      print("Ah, you have exact change... are you sure that this is the item that you'd like?")
      turtle.select(1)
      turtle.drop()
    elseif print("sorry pal, but it seems like you don't have a sufficient amount of funds.")
  -- After the identification, the turtle will print the message above, AND GIVE THE ITEM...
  -- Otherwise, it'll tell you that you will not be able to purchase the item.
  end
end
-- Next, i'm hoping to repeat the same process but with Gold, and Iron ingots.
function Gold_process()
turtle.select(6) 
-- This code will do the same as previously done... 
  turtle.getSelectedSlot(6)
    if turtle.getItemSpace(6) = c then
     print("Ah, you have exact change... are you sure that this  is te item that you' like?")
     turtle.select(2)
     turtle.drop()
  --Like before, the code will identify the slot, check the amount of items are in that slot, to see if the user has put the correct amount of cvurrency
  -- Afterards, the turtle will print the message, and see whether or not  the user has put a sufficient qamount of funds. 
  --If not, then the mesaage below will be printed.
  elseif print("I apologize to be the bearer of bad news but it seems like you do not have the sufficient amount of funds to purchase this item")
  end
end
-- And now, last but not least, Iron...
function Iron_process()
turtleselect(7)
--As previously done, this code is going to select, identify, and count items in the slot as well as the slot itself. 
  turtle.getSelectedSlot(7)
    if turtle.getItemSpace(7) = d then
      print("Ah why thank you for your patrionage, is that everything and are you sure that the selected item is in fact the one you'd like?" )
      then turtle.select(3)
      turtle.drop()
      --The iron item of your choice will be held in slot 3, if you have enough currency, the turtle will give you the item and if not you shall receive the message of rejection. 
      elseif print( "I apologize but you do not have the correct amunt of funds for this item...." )
     end
    end
 
Diaond_process()
Gold_process()
Iron_process()


